
enum Switch
	= On
    | Off;
let x = On;
let y = Off;

enum Nat
    = Z
    | S(prev: Nat);

let _0 = Z;
let _1 = S(_0);
let _2: Nat = S(_1);

let _error: Nat = 4;

enum OptionInt
    = ISome(value: Int)
    | INone;

let iSome = ISome(3);
let iNone = INone;
